# Production (Vercel) uses Neon when set. Omit locally to use embedded PGLite.
# DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Public origin used in sitemap / JSON-LD (optional)
# VITE_SITE_URL=https://lattice-beta-livid.vercel.app
